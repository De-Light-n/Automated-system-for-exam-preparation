# Підтримувані формати файлів

ExamNinja підтримує різні формати документів для завантаження та обробки навчальних матеріалів.

## 📄 Підтримувані формати

### 1. **PDF** (.pdf)
- ✅ Повна підтримка
- Бібліотека: `pdfjs-dist@3.11.174`
- Максимальний розмір: 10MB
- Підтримка: Багатосторінкові документи, текстовий шар

### 2. **Word Documents** (.docx)
- ✅ Повна підтримка
- Бібліотека: `mammoth@1.6.0`
- Максимальний розмір: 10MB
- Підтримка: Форматований текст, параграфи

### 3. **PowerPoint Presentations** (.pptx)
- ✅ Повна підтримка
- Бібліотека: `jszip@3.10.1`
- Максимальний розмір: 10MB
- Підтримка: Текст зі слайдів, зберігається порядок слайдів

### 4. **Plain Text** (.txt)
- ✅ Повна підтримка
- Нативна підтримка браузером
- Максимальний розмір: 10MB
- Підтримка: UTF-8 encoding

### 5. **Markdown** (.md)
- ✅ Повна підтримка
- Нативна підтримка браузером
- Максимальний розмір: 10MB
- Підтримка: Markdown синтаксис

## 🚫 Не підтримуються

- `.doc` (старі Word файли - використовуйте `.docx`)
- `.ppt` (старі PowerPoint - використовуйте `.pptx`)
- `.xls`, `.xlsx` (Excel таблиці)
- Зображення з текстом (OCR поки не підтримується)
- Аудіо/відео файли

## 🔧 Технічні деталі

### PDF обробка
```typescript
// Worker завантажується з CDN
workerUrl: 'https://unpkg.com/pdfjs-dist@3.11.174/build/pdf.worker.min.js'
```

### DOCX обробка
```typescript
// Витягує raw text з .docx
mammoth.extractRawText({ arrayBuffer })
```

### PPTX обробка
```typescript
// Розпаковує .pptx як ZIP і читає XML слайди
JSZip.loadAsync(file) → slides/*.xml
```

## 💡 Рекомендації

1. **PDF**: Найкраща сумісність для лекцій та підручників
2. **DOCX**: Ідеально для конспектів та есе
3. **PPTX**: Підходить для презентацій та слайдів
4. **TXT/MD**: Для швидких нотаток

## 🐛 Типові проблеми

### "PDF parser not initialized"
- Перевірте інтернет з'єднання (worker завантажується з CDN)
- Перезавантажте сторінку

### "Файл порожній"
- Переконайтесь що файл містить текст
- PDF з лише зображеннями не підтримуються (потрібен текстовий шар)

### "Формат файлу не підтримується"
- Використовуйте один з підтримуваних форматів
- Для старих `.doc` файлів - конвертуйте в `.docx`

## 📦 Встановлені пакети

```json
{
  "dependencies": {
    "pdfjs-dist": "3.11.174",
    "mammoth": "1.6.0",
    "jszip": "3.10.1"
  }
}
```
