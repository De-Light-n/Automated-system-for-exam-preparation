# 🎯 АРХІТЕКТУРА ДЕПЛОЮ

## До деплою (локально)

```
┌─────────────────────────────────────────┐
│         Твій комп'ютер                  │
│                                         │
│  ┌──────────┐              ┌─────────┐ │
│  │ Frontend │  fetch API   │ Backend │ │
│  │ :3000    │◄────────────►│ :5000   │ │
│  └──────────┘              └─────────┘ │
│                                  │      │
└──────────────────────────────────┼──────┘
                                   │
                                   ▼
                          ┌────────────────┐
                          │ MongoDB Local  │
                          └────────────────┘
```

---

## Після деплою (на Render)

```
┌──────────────────────────────────────────────────┐
│    https://examninja.onrender.com                │
│    (один домен для всього!)                      │
│                                                  │
│  ┌─────────────────────────────────────────┐    │
│  │         Render Web Service              │    │
│  │                                         │    │
│  │  ┌────────────┐      ┌──────────────┐  │    │
│  │  │  Frontend  │      │   Backend    │  │    │
│  │  │  (static)  │      │  (Express)   │  │    │
│  │  │            │      │              │  │    │
│  │  │ /          │◄────►│  /api/*      │  │    │
│  │  │ /about     │      │  /api/auth   │  │    │
│  │  │ /quiz      │      │  /api/chat   │  │    │
│  │  │            │      │  /api/quiz   │  │    │
│  │  └────────────┘      └──────────────┘  │    │
│  │                              │          │    │
│  └──────────────────────────────┼──────────┘    │
│                                 │               │
└─────────────────────────────────┼───────────────┘
                                  │
                                  │ mongoose
                                  │
                                  ▼
                    ┌──────────────────────────┐
                    │    MongoDB Atlas         │
                    │  (завжди онлайн)         │
                    │  cluster0.2ft8rbf...     │
                    └──────────────────────────┘
```

---

## Як працює Build Process

```
┌─────────────────────────────────────────┐
│  1. GitHub Push                         │
│     git push origin main                │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  2. Render автоматично                  │
│     - Клонує репо                       │
│     - cd server                         │
│     - npm install                       │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  3. Build Backend                       │
│     npm run build:backend               │
│     → tsc (TypeScript → JavaScript)     │
│     → створює server/dist/              │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  4. Build Frontend                      │
│     cd .. && npm ci && npm run build    │
│     → vite build                        │
│     → створює dist/                     │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  5. Copy Frontend → Backend             │
│     node scripts/copy-frontend.js       │
│     → копіює dist/ в server/dist/public/│
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  6. Start Server                        │
│     npm start                           │
│     → node dist/server.js               │
│     → роздає static з dist/public/      │
│     → обробляє API на /api/*            │
└─────────────────────────────────────────┘
```

---

## Структура після Build

```
server/
  dist/
    server.js          ← Backend код
    config/
    routes/
    models/
    public/            ← Frontend файли!
      index.html       ← Головна сторінка
      assets/
        index-abc.js   ← React код
        index-xyz.css  ← Стилі
      favicon.ico
```

---

## Routing

### Frontend Routes (React Router)

```
/ → index.html → React App
/quiz → index.html → React App (клієнтський роутинг)
/chat → index.html → React App (клієнтський роутинг)
```

### Backend Routes (Express)

```
/api/auth/*     → Authentication
/api/users/*    → User management
/api/materials/* → File uploads
/api/chat/*     → AI chat
/api/quiz/*     → Quiz generation
/api/health     → Health check
```

### Fallback

```
/* (все інше) → index.html
```

---

## Environment Variables Flow

```
┌─────────────────────┐
│  Render Dashboard   │
│                     │
│  NODE_ENV=production│
│  MONGODB_URI=...    │
│  JWT_SECRET=...     │
│  ...                │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  server.js          │
│                     │
│  process.env.PORT   │
│  process.env.JWT... │
└─────────────────────┘
```

Фронтенд НЕ бачить env змінних (вони тільки для backend)!

---

## Security

### ✅ Що захищено:

- HTTPS автоматично (Render SSL)
- Environment variables зашифровані
- MongoDB за паролем
- JWT токени
- CORS налаштовано
- Helmet.js (безпека headers)

### ⚠️ Що НЕ комітимо:

- `.env` файли
- `node_modules/`
- `dist/` (генерується)
- API keys
- Паролі

---

## Scaling

### Free Tier (поточний):

- 512 MB RAM
- 0.1 CPU
- Засинає через 15 хв
- Перший запит: ~30 сек

### Starter ($7/міс):

- 2 GB RAM
- 1 CPU
- Завжди активний
- Auto-scaling

---

## Monitoring

### Що дивитись:

1. **Logs** - помилки, запити
2. **Metrics** - CPU/RAM
3. **Deploy History** - коли що змінилось

### Корисні логи:

```
✅ Server is running on port 10000
✅ MongoDB connected
❌ Error: Connection refused
⚠️  Warning: High memory usage
```

---

## CI/CD Pipeline

```
Local Dev → Git Push → GitHub → Render → Production

           Auto Deploy    Auto Build   Auto Start
```

Кожен push = новий деплой (за ~5 хвилин)

---

**Повна інструкція:** `DEPLOY_ONE_SERVER_UA.md`
